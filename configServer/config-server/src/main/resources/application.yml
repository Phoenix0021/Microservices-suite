server:
  port: 8085


spring:
  application:
    name: CONFIG-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Phoenix0021/microservices-config
        clone-on-start: true